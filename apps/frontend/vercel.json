{
  "buildCommand": "cd ../.. && corepack enable && corepack prepare pnpm@latest --activate && pnpm install --frozen-lockfile && pnpm --filter @slooze/frontend build",
  "outputDirectory": ".next",
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@latest --activate && pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "rootDirectory": "apps/frontend"
}

